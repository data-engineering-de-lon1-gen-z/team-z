from extract import * #these *s will be replaced later with the names of all the functions being imported when we know what they are all called
from load import *
from transform import * 

if __name__ == "__main__":
    # TODO Call something from extract
    # data = extract_data()
    
    # TODO Pipe that into transform
    # transformed_data = transform_data(data)
    
    # TODO Load it
    # append_data(transformed_data)
    pass
